page { background: var(--es-bg-deep, #010407); }
.page {
  min-height: 100vh;
  padding: 24rpx;
  box-sizing: border-box;
  background: var(--es-bg-deep, #010407);
}
.card {
  background: var(--es-bg-deep, #010407);
  border-radius: 18rpx;
  padding: 24rpx;
  border: 1px solid rgba(0, 191, 255,0.18);
  box-shadow: 0 0 24rpx rgba(0, 191, 255,0.10);
  margin-bottom: 18rpx;
}
.title {
  color: var(--es-txt-1, #F4F7FA);
  font-size: 32rpx;
  font-weight: 800;
}
.sub {
  margin-top: 12rpx;
  color: rgba(255,255,255,0.65);
  font-size: 24rpx;
  word-break: break-all;
}
.btn {
  margin-top: 20rpx;
  border-radius: 14rpx;
  background: var(--es-green, #00BFFF);
  color: var(--es-bg-deep, #010407);
  font-size: 26rpx;
  font-weight: 700;
}
button::after {
  border: none;
}
.sectionTitle {
  margin-bottom: 12rpx;
  color: var(--es-green, #00BFFF);
  font-size: 26rpx;
  font-weight: 800;
}
.item {
  background: var(--es-on-green, #03060C);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 14rpx;
  padding: 18rpx;
  margin-bottom: 14rpx;
}
.line {
  display: flex;
  margin-bottom: 8rpx;
}
.k {
  width: 170rpx;
  flex: none;
  color: rgba(255,255,255,0.58);
  font-size: 24rpx;
}
.v {
  flex: 1;
  color: var(--es-txt-4, #7B8591);
  font-size: 24rpx;
  word-break: break-all;
}
.empty {
  text-align: center;
  color: rgba(255,255,255,0.58);
  font-size: 24rpx;
}

.tracker-entry {
  margin-top: 24rpx;
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
}

.tracker-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 18rpx;
  background: linear-gradient(90deg, var(--es-safe, #00E5A0), var(--es-green, #00BFFF));
  color: var(--es-bg-deep, #010407);
  font-size: 28rpx;
  font-weight: 600;
  text-decoration: none;
  box-sizing: border-box;
}

.tracker-tip {
  display: block;
  margin-top: 10rpx;
  text-align: center;
  font-size: 22rpx;
  color: var(--es-txt-2, #AAB4BE);
}
