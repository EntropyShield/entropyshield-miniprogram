<view class="page risk-calculator">
  <view class="title-container">
    <text class="title">风控计算器</text>
    <text class="sub-title">根据你的资金与首次买入价格，快速生成风控方案。</text>
  </view>

  <view class="quick-tip-card">
    <text class="quick-tip-title">先填 3 项即可开始</text>
    <text class="quick-tip-text">请填写「可用资金」「首次买入价格」「标的代码或名称」。首次使用建议先算稳健版。</text>
  </view>

  <view class="rights-banner">
    <view class="rights-row">
      <text class="rights-label">会员权益：</text>
      <text class="rights-text">{{membershipName || '未开通'}}</text>
    </view>

    <view class="rights-row">
      <text class="rights-label">到期状态：</text>
      <block wx:if="{{membershipName}}">
        <text class="rights-text" wx:if="{{remainingDays > 0}}">
          剩余 {{remainingDays}} 天
        </text>
        <text class="rights-text" wx:else>
          已到期
        </text>
      </block>
      <text class="rights-text" wx:else>—</text>
    </view>

    <view class="rights-row">
      <text class="rights-label">任务权益：</text>
      <text class="rights-text">{{freeCalcTimes}} 次</text>
    </view>
  </view>

  <view class="input-container">
    <view class="input-field">
      <view class="label-row">
        <text class="input-label">可用资金（元）</text>
        <text class="label-tag required-tag">必填</text>
      </view>
      <input
        class="input {{balanceError ? 'input-error' : ''}}"
        type="digit"
        placeholder="例如：100000"
        bindinput="onBalanceInput"
        value="{{balance}}"
        confirm-type="next"
      />
      <text class="field-tip">填写你这次准备参与计算的可用资金。</text>
      <text class="field-error" wx:if="{{balanceError}}">{{balanceError}}</text>
    </view>

    <view class="input-field">
      <view class="label-row">
        <text class="input-label">首次买入价格</text>
        <text class="label-tag required-tag">必填</text>
      </view>
      <input
        class="input {{priceError ? 'input-error' : ''}}"
        type="digit"
        placeholder="例如：12.36"
        bindinput="onPriceInput"
        value="{{price}}"
        confirm-type="next"
      />
      <text class="field-tip">填写你计划第一次建仓的价格。</text>
      <text class="field-error" wx:if="{{priceError}}">{{priceError}}</text>
    </view>

    <view class="input-field">
      <view class="label-row">
        <text class="input-label">标的代码或名称</text>
        <text class="label-tag required-tag">必填</text>
      </view>
      <input
        class="input {{codeError ? 'input-error' : ''}}"
        placeholder="例如：000001 或 平安银行"
        bindinput="onCodeInput"
        value="{{code}}"
        confirm-type="done"
      />
      <text class="field-tip">必填，用于后续记录、统计与跟踪。</text>
      <text class="field-error" wx:if="{{codeError}}">{{codeError}}</text>
    </view>
  </view>

  <view class="button-container">
    <text class="cta-tip">首次使用建议先算稳健版，先完成一次完整计算。</text>

    <view class="btn-primary" bindtap="onClickSteady">
      <text class="btn-text">先算稳健版（推荐）</text>
      <text class="btn-icon">→</text>
    </view>

    <view class="btn-secondary" bindtap="onClickAdvanced">
      <text class="btn-text btn-text-secondary">再看加强版</text>
      <text class="btn-icon btn-icon-secondary">→</text>
    </view>

    <text class="advanced-note" wx:if="{{advancedEnabled}}">
      当前会员权益可直接使用加强版。
    </text>
    <text class="advanced-note" wx:else>
      加强版适合进阶用户；若会员权益不支持，会自动引导开通。
    </text>
  </view>

  <view class="btn-back" bindtap="goHome">
    返回首页
  </view>
</view>